// TODO: What will the following line of code log?
console.log(window);
//It will log the window in the console.

// TODO: What will the following line of code log?
console.log(window.document);
//This logs the entire document in the console.

// TODO: What will the following line of code log?
console.log(document.documentElement);
//This logs the specific document elements in the window.

// TODO: What will the following line of code log?
console.log(document.head);
//This logs only the head tag and what's inside it within the document.

console.log(Object.keys(window).length);
console.log(Object.keys(window));
